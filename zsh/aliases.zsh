# =============================================================================
# ALIASES
# =============================================================================

# Add any simple aliases here
# Example:
# alias ll='ls -la'
# alias ..='cd ..'

# Source external alias files if they exist
[ -f $HOME/.sbn_aliases ] && source $HOME/.sbn_aliases
[ -f $HOME/code/cis-ai/ezra/claude/aliases.sh ] && source $HOME/code/cis-ai/ezra/claude/aliases.sh